# learn-ops-api: AI Prompts

> **Context for this whole conversation:**
> You are helping me explore `learn-ops-api`, a Django REST API project. I am new to this codebase. Read the repository directly. There is a file called `module-exploration-ai.md` in the same directory — instead of showing me answers in chat, directly edit and fill in that file. Keep each answer concise and high-signal.

---

## 1. Project structure

**Prompt for Questions 1–2**
> Read the project structure and fill in `module-exploration-ai.md` for Questions 1 and 2:
>
> **Q1.** For each important top-level folder, fill in the table row: why does it need to exist?
>
> **Q2.** For each important folder inside `LearningAPI/`, fill in the table row: what responsibility does it own and why?
>
> Edit the file directly.

---

## 2. Dependencies

**Prompt for Questions 3–4**
> Open `Pipfile` and fill in `module-exploration-ai.md` for Questions 3 and 4:
>
> **Q3.** Write a short paragraph: what is the `Pipfile` and why does it exist?
>
> **Q4.** Fill in the table rows for `django`, `djangorestframework`, and `django-allauth`.
>
> Edit the file directly.

---

## 3. Decorators, serializers, and models

**Prompt for Questions 5–7**
> Read the relevant files and fill in `module-exploration-ai.md` for Questions 5, 6, and 7:
>
> **Q5.** What does `decorators.py` do and what problem does it solve?
>
> **Q6.** What is a serializer in DRF and how does it fit the request/response cycle? Name one concrete serializer from this project.
>
> **Q7.** Pick one model from `models/`. What real-world thing does it represent and why does the API need to track that data?
>
> Edit the file directly.

---

## 4. Views, viewsets, and the MTV pattern

**Prompt for Questions 8–9**
> Read the relevant view files and fill in `module-exploration-ai.md` for Questions 8 and 9:
>
> **Q8.** Fill in the table with one plain View and one ViewSet from this project, and explain when to use each.
>
> **Q9.** Django uses Model-Template-View, but this project has no HTML templates. What replaces the template's job, and why does that design make sense?
>
> Edit the file directly.


